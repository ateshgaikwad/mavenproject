pipeline{
    agent any
    stages{
        stage("srm checkout"){
            steps{withMaven(globalMavenSettingsConfig: '', jdk: '', maven: 'Maven', mavenSettingsConfig: '', traceability: true)}
            {
                sh 'mvn compile'
        }
    }
}
}
